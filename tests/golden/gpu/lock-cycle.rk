import "gpu"

fn main() (error?) {
    check gpu.lock("default", "golden cycle a")
    print("locked")
    check gpu.unlock("default")
    check gpu.lock("default", "golden cycle b")
    print("locked again")
    check gpu.unlock("default")
    print("done")
    return none
}
