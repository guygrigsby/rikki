import "gpu"

fn main() (error?) {
    check gpu.shared("low holder")
    ok, err := gpu.trylock("training probe")
    if err != none {
        return err
    }
    print(ok)
    check gpu.unlock()
    return none
}
