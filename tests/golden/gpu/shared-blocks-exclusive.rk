import "gpu"

fn main() (error?) {
    check gpu.shared("default", "low holder")
    ok, err := gpu.trylock("default", "training probe")
    if err != none {
        return err
    }
    print(ok)
    check gpu.unlock("default")
    return none
}
