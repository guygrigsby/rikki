fn main() {
    total := 0
    xs := [1, 2, 3]
    f := fn(x int) {
        total = total + x
        xs[0] = x
        local := x
        local = local + 1
        print(local)
    }
    f(1)
    print(total)
}
